/*
 * request.hpp
 *
 * This header file defines the structures and functions necessary to handle
 * HTTP/1.1 requests. It provides functionality to interpret 
 * HTTP headers, methods, status lines, and message bodies, enabling seamless
 * integration into HTTP servers or clients.
 *
 * Features:
 * - Parses HTTP/1.1 request lines (method, URI, version)
 * - Parses HTTP/1.1 response status lines (version, status code, reason phrase)
 * - Extracts and stores HTTP headers and message body
 *
 * Usage:
 * Include this file in your project to add HTTP request handler.
 *
 * Author: Yousef.smt
 * Date: 02-Jul-2024
*/

#ifndef HTTP_INCLUDE_REQUEST_H_
#define HTTP_INCLUDE_REQUEST_H_

namespace http
{
    
} // namespace http

#endif